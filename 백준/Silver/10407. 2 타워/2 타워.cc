#include <iostream>
using namespace std;

int main() {
    cin.tie(nullptr);   ios_base::sync_with_stdio(false);

    string s;
    cin >> s;

    if (s == "1") cout << 2;
    else cout << 1;
    
    return 0;
}